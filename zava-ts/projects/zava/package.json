{
  "name": "zava",
  "version": "0.0.1",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "test": "ng test --no-watch --code-coverage",
    "lint": "ng lint",
    "init": "npm install & npm-install-peers",
    "compodoc": "npx compodoc -p tsconfig.lib.json -n zava --hideGenerator --customFavicon compodoc/favicon.ico --customLogo compodoc/logo.png src"
  },
  "peerDependencies": {
    "@angular/common": "^11.0.4",
    "@angular/core": "^11.0.4"
  },
  "dependencies": {
    "tslib": "^2.0.0"
  },
  "devDependencies": {
    "@types/mathjs": "^6.0.8",
    "mathjs": "^8.1.0"
  }
}
