import { TestBed } from '@angular/core/testing';

import { GrandTour } from './zava.core';

describe('ZavaCore', () => {

  it('should do nothing', () => {
    const grandTour = new GrandTour();
  });
});
